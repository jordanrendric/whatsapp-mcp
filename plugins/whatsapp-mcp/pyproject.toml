[project]
name = "whatsapp-mcp"
version = "0.1.0"
description = "Read-only WhatsApp for macOS with local Whisper transcription"
requires-python = ">=3.11"
dependencies = ["mcp>=1.12.4,<2"]

[tool.uv]
package = false
